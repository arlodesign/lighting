{
  "name": "lighting",
  "source": "src/index.html",
  "scripts": {
    "start": "parcel",
    "build": "rm -rf dist && parcel build",
    "prepare": "husky"
  },
  "devDependencies": {
    "husky": "^9.1.5",
    "lint-staged": "^15.2.10",
    "parcel": "latest",
    "prettier": "3.3.3"
  },
  "dependencies": {
    "p5": "^1.10.0"
  },
  "lint-staged": {
    "**/*": "prettier --write --ignore-unknown"
  }
}
